// <auto-generated />
// version: 1.0.0
// abi_hash: 5e6878cb4cb395d1a3154ead424735a170510dadf75d5a8d7c8175e7b867461b
using System;
using System.Text;

namespace RustedGeom.Generated;

public sealed class RgmException : Exception
{
    public RgmStatus Status { get; }

    public RgmException(RgmStatus status, string message) : base(message)
    {
        Status = status;
    }
}

internal static unsafe class NativeError
{
    internal static void ThrowIfError(RgmStatus status, RgmKernelHandle session, string context)
    {
        if (status == RgmStatus.Ok)
        {
            return;
        }

        var message = TryGetLastErrorMessage(session);
        if (string.IsNullOrWhiteSpace(message))
        {
            message = context;
        }

        throw new RgmException(status, $"{context}: {message}");
    }

    private static string TryGetLastErrorMessage(RgmKernelHandle session)
    {
        var status = NativeMethods.InteropLastErrorMessage(session, null, 0, out nuint written);
        if (status != RgmStatus.Ok || written == 0)
        {
            return string.Empty;
        }

        var buffer = new byte[(int)written + 1];
        fixed (byte* ptr = buffer)
        {
            status = NativeMethods.InteropLastErrorMessage(session, ptr, (nuint)buffer.Length, out written);
            if (status != RgmStatus.Ok || written == 0)
            {
                return string.Empty;
            }
            return Encoding.UTF8.GetString(buffer, 0, (int)written);
        }
    }
}

public sealed unsafe class KernelHandle : IDisposable
{
    private RgmKernelHandle _session;
    private bool _disposed;

    private KernelHandle(RgmKernelHandle session)
    {
        _session = session;
    }

    internal RgmKernelHandle Session => _session;

    public static KernelHandle Create()
    {
        var status = NativeMethods.InteropKernelCreate(out var session);
        NativeError.ThrowIfError(status, session, "Kernel.Create");
        return new KernelHandle(session);
    }

    public void Dispose()
    {
        if (_disposed)
        {
            return;
        }

        var status = NativeMethods.InteropKernelDestroy(_session);
        if (status != RgmStatus.Ok && status != RgmStatus.NotFound)
        {
            NativeError.ThrowIfError(status, _session, "Kernel.Dispose");
        }

        _disposed = true;
    }

    public CurveHandle InterpolateNurbsFitPoints(ReadOnlySpan<RgmPoint3> points, uint degree, bool closed, RgmToleranceContext tol)
    {
        if (points.Length == 0)
        {
            throw new ArgumentException("At least one point is required", nameof(points));
        }

        fixed (RgmPoint3* pointsPtr = points)
        {
            var status = NativeMethods.InteropNurbsInterpolateFitPoints(_session, pointsPtr, (nuint)points.Length, degree, closed, tol, out var curve);
            NativeError.ThrowIfError(status, _session, "Kernel.InterpolateNurbsFitPoints");
            return new CurveHandle(this, curve);
        }
    }
}

public sealed unsafe class CurveHandle : IDisposable
{
    private readonly KernelHandle _kernel;
    private RgmObjectHandle _curve;
    private bool _disposed;

    internal CurveHandle(KernelHandle kernel, RgmObjectHandle curve)
    {
        _kernel = kernel;
        _curve = curve;
    }

    public void Dispose()
    {
        if (_disposed)
        {
            return;
        }

        var status = NativeMethods.InteropObjectRelease(_kernel.Session, _curve);
        if (status != RgmStatus.Ok && status != RgmStatus.NotFound)
        {
            NativeError.ThrowIfError(status, _kernel.Session, "Curve.Dispose");
        }

        _disposed = true;
    }

    private void ThrowIfDisposed()
    {
        if (_disposed)
        {
            throw new ObjectDisposedException(nameof(CurveHandle));
        }
    }

    public RgmPoint3 D0(double tNorm)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveD0At(_kernel.Session, _curve, tNorm, out var outPoint);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.D0");
        return outPoint;
    }

    public RgmPoint3 D0AtLength(double distanceLength)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveD0AtLength(_kernel.Session, _curve, distanceLength, out var outPoint);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.D0AtLength");
        return outPoint;
    }

    public RgmVec3 D1(double tNorm)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveD1At(_kernel.Session, _curve, tNorm, out var outD1);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.D1");
        return outD1;
    }

    public RgmVec3 D1AtLength(double distanceLength)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveD1AtLength(_kernel.Session, _curve, distanceLength, out var outD1);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.D1AtLength");
        return outD1;
    }

    public RgmVec3 D2(double tNorm)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveD2At(_kernel.Session, _curve, tNorm, out var outDerivative);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.D2");
        return outDerivative;
    }

    public RgmVec3 D2AtLength(double distanceLength)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveD2AtLength(_kernel.Session, _curve, distanceLength, out var outDerivative);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.D2AtLength");
        return outDerivative;
    }

    public RgmVec3 NormalAt(double tNorm)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveNormalAt(_kernel.Session, _curve, tNorm, out var outNormal);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.NormalAt");
        return outNormal;
    }

    public RgmVec3 NormalAtLength(double distanceLength)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveNormalAtLength(_kernel.Session, _curve, distanceLength, out var outNormal);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.NormalAtLength");
        return outNormal;
    }

    public RgmPlane PlaneAt(double tNorm)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurvePlaneAt(_kernel.Session, _curve, tNorm, out var outPlane);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.PlaneAt");
        return outPlane;
    }

    public RgmPlane PlaneAtLength(double distanceLength)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurvePlaneAtLength(_kernel.Session, _curve, distanceLength, out var outPlane);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.PlaneAtLength");
        return outPlane;
    }

    public RgmPoint3 PointAt(double tNorm)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurvePointAt(_kernel.Session, _curve, tNorm, out var outPoint);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.PointAt");
        return outPoint;
    }

    public RgmPoint3 PointAtLength(double distanceLength)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurvePointAtLength(_kernel.Session, _curve, distanceLength, out var outPoint);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.PointAtLength");
        return outPoint;
    }

    public RgmVec3 TangentAt(double tNorm)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveTangentAt(_kernel.Session, _curve, tNorm, out var outTangent);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.TangentAt");
        return outTangent;
    }

    public RgmVec3 TangentAtLength(double distanceLength)
    {
        ThrowIfDisposed();
        var status = NativeMethods.InteropCurveTangentAtLength(_kernel.Session, _curve, distanceLength, out var outTangent);
        NativeError.ThrowIfError(status, _kernel.Session, "Curve.TangentAtLength");
        return outTangent;
    }
}
